{{#partial "content"}}
    {{#if username}}
        <h1>Welcome {{username}}!</h1>
        <p>View <a href="/ideas">all course ideea</a>.</p><!--link la toate cursurile DAO, daca nespecificat => GET -->
    {{else}}
        <h1>Welcome Students</h1>
        <form action="/sign-in" method="post">
            <input type="text" placeholder="What's your user name" name="username"/>
            <button>Sign in</button>
        </form>
    {{/if}}
{{/partial}}

{{>base.hbs}} <!--link la template cu header si footer-->
